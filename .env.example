# Copy to .env and adjust as needed. docker-compose reads SESSION_SECRET
# from the environment (or this file if you `export`/source it).

# Signs the fake-login session cookie.
SESSION_SECRET=change-me-for-anything-non-local

# Used later for the AI chat feature (not required by PREL-4).
OPENROUTER_API_KEY=
